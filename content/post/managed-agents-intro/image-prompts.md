# 文章配图生成 Prompt

生成后将图片保存到本目录，格式为 .webp 或 .png（会自动转为 .webp），文件名与文章中引用一致。

---

## 1. cover.webp — 文章封面

A sleek dark-mode tech illustration showing a transformation/evolution concept. Left side: a simple glowing chat bubble icon (representing a basic chatbot), connected by a luminous teal (#2dd4bf) arrow/bridge to the right side: a complex autonomous agent represented as a robotic figure with multiple tool arms (terminal, file, globe, code icons) working independently. The bridge/arrow has particle effects showing the transition from simple to powerful. Background: deep navy (#07090f) with subtle hexagonal grid pattern. Accent colors: teal (#2dd4bf) primary, amber (#f59e0b) secondary, purple (#a78bfa) tertiary. Clean tech illustration, dark mode aesthetic, minimal and elegant. No text. 16:9 aspect ratio.

---

## 2. handcraft-arch.webp — 手搓版架构图

A minimal dark-mode architecture diagram showing a simple request-response flow. Left: a browser icon with a small chat panel. Center: a single small serverless function box (glowing teal outline on dark #0d1117 background). Right: an API cloud icon (OpenAI). Simple straight arrows connecting them: browser → function → API → function → browser. A small YAML file icon attached below the function box. Everything simple, minimal, showing how lightweight the setup is. Style: clean technical diagram, dark background (#07090f), teal (#2dd4bf) lines and outlines. No text. 16:9 aspect ratio.

---

## 3. v1-vs-v2.webp — v1 vs v2 对比

A dramatic split-screen dark illustration comparing two paradigms. Left half (dimmer, simpler): a small chat bubble floating alone with a single thin connection line to a cloud — representing the basic chatbot (v1). Right half (brighter, more complex): a central agent orb radiating multiple tool connections — bash terminal, file system, web globe, code brackets, and sub-agent orbs — all connected by glowing teal (#2dd4bf) and amber (#f59e0b) lines, representing Managed Agents (v2). A subtle dividing line or gradient separates the two halves. Background: deep dark (#07090f). Style: conceptual tech comparison, dramatic lighting contrast. No text. 16:9 aspect ratio.

---

## 4. multi-agent.webp — 多 Agent 编排

A dark-mode illustration showing multi-agent orchestration. Center top: a larger "coordinator" agent orb (teal glow, #2dd4bf). Below it, three smaller specialized agent orbs arranged in a row: one with a document icon (blog agent, purple #a78bfa), one with a code brackets icon (code agent, amber #f59e0b), one with a search/globe icon (research agent, teal #2dd4bf). Dotted delegation lines from coordinator to each sub-agent. All agents sit on a shared platform/container base (subtle rectangle outline). Each sub-agent has its own small context bubble. Background: deep dark (#07090f) with subtle grid. Style: clean orchestration diagram, dark mode, minimal. No text. 16:9 aspect ratio.

---

## 5. practice-arch.webp — 混合方案实战架构

A dark-mode architecture diagram showing a hybrid routing pattern. Top: a browser icon. Below it: a lightweight gateway box (small, teal outline). From the gateway, two paths branch: left path (simpler, lighter arrow) leads to "Messages API" (small simple box) — for casual chat; right path (thicker, brighter arrow) leads to "Managed Agents" (larger complex box with tool icons inside) — for complex tasks. A small decision diamond between the gateway and the two paths. Background: deep navy (#07090f). Lines in teal (#2dd4bf) and amber (#f59e0b). Style: clean technical flow diagram, dark mode aesthetic. No text. 16:9 aspect ratio.

---

## 使用说明

1. 将上述 prompt 分别输入 AI 图片生成工具（如 Midjourney, DALL-E, Ideogram 等）
2. 建议使用 16:9 比例，分辨率至少 1920x1080
3. 生成后保存到本目录（PNG 或 WebP 均可，后续会统一转为 WebP）
4. 文章中已经用 `![描述](文件名.webp)` 格式引用了这些图片
