# 文章配图生成 Prompt

生成后将封面图保存到本目录，命名为 `cover.png` 或 `cover.webp`（PNG 会自动转为 WebP）。

---

## cover.webp — 文章封面

A clean, minimal tech illustration on a deep dark navy background (#07090f). The central visual is a self-correcting agent loop: a glowing teal (#2dd4bf) circular arrow forming a closed loop, flowing from an "AGENT" node (a softly glowing rounded monitor icon) through a row of small "WORKER" nodes, into a diamond-shaped "GRADE" checkpoint, then looping back to the agent. The loop arrow is rendered in teal with a subtle gradient glow. Secondary accent in amber (#f59e0b) highlights the grade/checkpoint diamond. A faint purple (#a78bfa) memory layer is suggested underneath as a horizontal dotted line spanning across sessions. Dark mode aesthetic, minimal and elegant, lots of negative space, soft ambient glow, thin precise line work, futuristic but calm. Composition weighted to the right two-thirds, left third kept clean for visual breathing room.

Text allowed: include the short title "DESIGNING LOOPS" in a small, clean monospace font in teal, placed in the lower-left area. Keep text minimal and legible.

16:9 aspect ratio.

---

## 使用说明

1. 将上述 prompt 输入 AI 图片生成工具（Midjourney / DALL-E / Ideogram / Nano Banana 等）
2. 比例 16:9，分辨率至少 1920x1080
3. 生成后保存到本目录（命名 cover.png 或 cover.webp 均可，我会统一转换）
4. 文章中已用 `![Designing loops with Fable 5](cover.webp)` 引用
