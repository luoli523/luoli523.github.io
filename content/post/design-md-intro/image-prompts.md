# 文章配图生成 Prompt

本文共 **4 张图**，其中 **2 张为真实截图**（你手动截图），**2 张为 AI 生成图**。

---

## 1. cover.webp — 文章封面（AI 生成）

A clean tech illustration on deep dark navy background (#07090f). Center: a large glowing markdown document icon with the visible text "DESIGN.md" at the top of the document (in elegant monospace font, teal glow #2dd4bf). Radiating outward from the document: multiple floating UI cards/mockups in different distinctive styles — one warm terracotta editorial card (Claude-style), one stark black-and-white minimal card (Vercel-style), one purple-accented task card (Linear-style), one cream paper-textured card (Notion-style). Each mockup is semi-transparent and connected to the central document with thin glowing lines, suggesting "one source, many outputs." The overall composition is minimal and elegant, dark mode aesthetic, 16:9. The text "DESIGN.md" IS allowed on the central document. No other text. 16:9 aspect ratio.

---

## 2. repo-screenshot.webp — awesome-design-md 仓库截图（**真实截图**）

**请打开** https://github.com/VoltAgent/awesome-design-md **并截图**，要求：

- 显示仓库顶部的 star 数（51k）、fork 数、watch 数
- 显示仓库名 "VoltAgent/awesome-design-md"
- 能看到 README 顶部的 banner / 标题 "Awesome DESIGN.md"
- 尽量包含 "DESIGN.md Count: 66" 这个 badge
- 可以是浏览器窗口完整截图，或聚焦于 repo header 区域
- 保存为 `repo-screenshot.png`（或 .webp）到本目录

---

## 3. claude-design-md-sample.webp — Claude 品牌 DESIGN.md 真实内容（**真实截图**）

**请打开** https://getdesign.md/claude/design-md **并截图**，要求：

- 截取展示 Claude DESIGN.md 实际内容的部分（比如色板、typography、component 规范）
- 最好能看到 Claude 标志性的 terracotta 色值 (#d97757) 或 cream white (#faf9f5)
- 截图能体现 "markdown 文档 + 设计 token 展示" 的直观感受
- 如果 getdesign.md 打不开，可以用 GitHub 仓库里的对应文件替代：https://github.com/VoltAgent/awesome-design-md/tree/main/design-md/claude
- 保存为 `claude-design-md-sample.png`（或 .webp）到本目录

---

## 4. generated-ui-compare.webp — 有/没有 DESIGN.md 的 UI 对比（AI 生成）

A side-by-side comparison illustration on deep dark navy background (#07090f). Left side labeled "Without DESIGN.md": a generic, bland Bootstrap-style pricing page mockup with default blue buttons, Arial-like font, plain white background, generic card layout — looks technically correct but soulless. Right side labeled "With DESIGN.md": the same pricing page but reskinned with clear Linear-style aesthetic — deep purple (#5e6ad2) accents, Inter font with tight tracking, subtle glow on buttons, dark mode surface, elegant spacing. A thin vertical divider separates the two sides. Both mockups are clean product illustrations, not photos. The contrast is obvious and tells a story. Minor small labels "Without DESIGN.md" and "With DESIGN.md" are allowed. 16:9 aspect ratio.

---

## 使用说明

1. 图 1 和图 4 用 AI 图片生成工具（Midjourney / DALL-E / Ideogram）生成
2. 图 2 和图 3 直接截图
3. 所有图片保存到本目录（`content/post/design-md-intro/`）
4. PNG 或 WebP 均可，我会统一转成 WebP
5. 文章已用 `![描述](文件名.webp)` 格式引用了所有 4 张图
